---
- hosts: 172.31.7.165
  become: yes
  become_method: sudo
  tasks:
    - name: Remove the volume group.
      lvg:
        vg: test-lvm
        state: absent

    - name: Remove the PV volume group.
      shell: pvremove /dev/xvdf /dev/xvdg
